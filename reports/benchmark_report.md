# Benchmark Report

This file is regenerated with measured values by:

```bash
python -m multi_agent_research_lab.cli benchmark \
  --query "Compare single-agent and multi-agent research systems and explain when each is preferable"
```

The manual GitHub Actions `live` mode also generates this report and uploads it as an artifact. No benchmark values are fabricated before a real provider run.
